name: styled_widget
description: Simplifying your widget tree structure by using a method based widget tree structure, inspired by SwiftUI.
version: 0.0.3
homepage: https://github.com/ReinBentdal/styled_widget
repository: https://github.com/ReinBentdal/styled_widget

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

analyzer:
  enable-experiment: extension-methods